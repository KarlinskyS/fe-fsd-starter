import { initialFeature } from './initialFeature';
import { homePage } from './homePage';
import { languageSwitcher } from './languageSwitcher';

export const en = {
  homePage,
  initialFeature,
  languageSwitcher,
} as const;
