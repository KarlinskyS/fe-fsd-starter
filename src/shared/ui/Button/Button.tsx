import { default as BaseButton, type ButtonProps as BaseButtonProps } from '@mui/material/Button';

export type ButtonProps = BaseButtonProps;
export const Button = BaseButton;
export default Button;
